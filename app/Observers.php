<?php
namespace App\Observers;

use App\Notifications\NewItem;
// use App\Item;
use App\User;

class ItemObserver
{
    public function created()
    {
        // $author = $item->user;
        // $users = User::all();
        // foreach ($users as $user) {
        //     $user->notify(new NewItem($item,$author));
        // }

        // $newTransaction = notify(new NewItem());
        
    }
}